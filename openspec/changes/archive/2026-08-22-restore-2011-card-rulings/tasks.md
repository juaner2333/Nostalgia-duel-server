## 1. 建立失败回归

- [x] 1.1 在现有 YGOPro 测试目录增加历史裁定测试辅助器，使用固定 CDB、format-first 脚本 reader 和真实 `koishipro-core.js` WASM 构造最小决斗场面
- [x] 1.2 增加覆盖清单测试，断言 1103/1109 的目标 ID 集合均为本变更的 12 张、同名脚本摘要一致且不会跨 format 查找，并确认测试在脚本尚未加入时失败
- [x] 1.3 为第一批 11 张卡增加发动次数、检索限制、对象、处理时点、表示形式和效果顺序的失败 WASM 场景，并确认现代 base 至少在每个规范场景中产生预期的失败差异
- [x] 1.4 为「王家长眠之谷」增加 A 版墓地处理的失败 WASM 场景（禁止墓地除外、不取对象自身移动例外、取对象自身移动无效、墓地效果只影响卡组），并确认现代 base 产生预期的失败差异

## 2. 恢复第一批历史裁定

- [x] 2.1 依据审核来源在 1103 format 中实现 `95727991`、`26202165`、`50321796`、`88264978`、`70583986`、`25862681` 与 `96782886` 的 2011 发动次数、检索和成本语义
- [x] 2.2 依据审核来源在 1103 format 中实现 `77565204`、`21502796`、`80168720` 与 `16226786` 的 2011 对象、时点、表示形式和处理顺序（`5861892` 保持现代 base 裁定，不在本变更范围）
- [x] 2.3 运行第一批聚焦 WASM 测试，修正兼容问题但不带回后续勘误语义，确认 11 张卡的全部规范场景通过

## 3. 王家长眠之谷裁定

- [x] 3.1 依据 ProjectIgnis `pre-errata/c511002998.lua` 与 Fluorohydride `0c3fd99^`，在 1103 format 中实现 `47355498` 的 2011 OCG A 版裁定，并保留不改变裁定的后续引擎兼容修正
- [x] 3.2 运行王家长眠之谷聚焦 WASM 测试，确认 A 版四个规范场景通过

## 4. 完成双环境覆盖与隔离验证

- [x] 4.1 将审核通过的 12 个 1103 脚本逐字节复制到 1109 format，并移除已不需要的两个 `.gitkeep`
- [x] 4.2 运行覆盖清单、逐文件摘要与 format 隔离测试，确认两个环境各自优先加载本环境覆盖且不读取另一环境
- [x] 4.3 分别用 1103 和 1109 脚本链运行历史裁定聚焦套件，确认相同场景得到等价的 2011 结果

## 5. 更新并审核固定资源锁

- [x] 5.1 在全部脚本和聚焦测试通过后执行 `npm run generate:nostalgia-lock`，不得手工编辑 `nostalgia-resources/lock.json`
- [x] 5.2 审核 lock 差异，确认两个 format 的脚本数均为 12，base 数据库/card ID 摘要、两份 LFList、卡池数量和运行时规则保持不变
- [x] 5.3 执行 `npm run check:nostalgia-resources`，确认脚本摘要、资源边界和固定资源完整性全部通过

## 6. 完整回归与交付

- [x] 6.1 运行历史裁定聚焦测试和相关 YGOPro 资源/WASM 测试，确认无聚焦回归
- [x] 6.2 运行 `npm run lint`、`npm run test` 与 `npm run build` 并修复本变更造成的失败
- [x] 6.3 对已启动的固定资源实例运行 `npm run smoke:duel`，确认 1103/1109 建房、卡组校验、真实 WASM 决斗和观战流程通过
- [x] 6.4 检查实现差异只包含历史裁定测试、两个 format 的 12 个脚本、机械生成的资源锁和本变更规划制品，并整理每张卡采用的历史来源供最终台账使用

## 7. 补全历史裁定台账

- [x] 7.1 在全部脚本和验证完成后创建 `docs/historical-card-rulings.md`，分别记录已修复脚本（12 张）、后续候选脚本（第二批 14 张与光之支配者）和旧裁定来源；逐项核对 12 个目标 ID、卡名、适用环境、裁定差异、脚本路径、固定来源与验证状态，并明确候选项尚未进入运行时
