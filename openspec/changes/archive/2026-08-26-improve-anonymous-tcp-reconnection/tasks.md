## 1. 固定普通 TCP 重连资格

- [x] 1.1 先扩展 `findReconnectingPlayer` 就近测试，覆盖同 IP 且旧 TCP socket 仍打开时可以匹配、同 IP 且已关闭时仍可匹配、不同 IP 拒绝、任一端为 WebSocket 时不走半开接管、强认证玩家仍不可按名称接管，并确认新增用例在生产代码修改前失败。
- [x] 1.2 在 `ISocket` 及 TCP/WebSocket 适配器中增加显式只读传输类型，在玩家对象上暴露构造时缓存并在换 socket 时维护的稳定来源 IP；同步最小更新测试桩，不引入具体适配器反向依赖。
- [x] 1.3 将共享重连资格判定改为返回玩家或稳定拒绝原因；仅对原/新均为普通 TCP 的 casual 玩家移除 `closed` 条件，同时保留精确昵称、稳定 IP、非强认证和既有 ranked 行为，并使 1.1 聚焦测试通过。

## 2. 安全替换半开旧连接

- [x] 2.1 先为房间重连入口增加失败测试，断言旧 socket 的房间消息/关闭回调先被解除、旧底层连接被销毁、新 socket 绑定到原玩家且玩家位置/队伍/主机状态不变，旧连接迟到的 close 不触发房间清理。
- [x] 2.2 外科手术式修改统一房间重连入口：同步捕获并解除旧 socket、主动销毁旧连接、绑定新连接、标记重连并发送既有初始化消息；加入同一 socket 的幂等保护，并使 2.1 测试通过。
- [x] 2.3 增加连续两个同 IP、同房间、同昵称连接的测试，确认按处理顺序执行 `last join wins`、中间连接被关闭且最终只有最新 socket 能向原玩家发送房间命令。

## 3. 接入各已开局状态与诊断日志

- [x] 3.1 先在猜拳、选择先后手、决斗中和换备状态的就近测试中覆盖同 IP 半开 TCP 接管与不同 IP 降级路径，固定各阶段已有 DUEL_START、卡组计数、选择提示、卡组重新提交和场面同步行为。
- [x] 3.2 将四个状态处理器接入带传输类型和稳定 IP 的共享资格结果及统一 socket 替换入口，不改变等待、观战或 token 重连分支，并使 3.1 测试通过。
- [x] 3.3 增加并验证结构化日志：成功接管或拒绝时记录稳定结果原因、组合房间身份、阶段和新旧 socket ID，不记录 token、完整 JoinGame/PlayerInfo 或卡组十六进制载荷。

## 4. 增加全部玩家断线宽限期

- [x] 4.1 先使用 Jest fake timers 为房间和断线处理增加失败测试，覆盖已开局普通房间全部玩家断线后保留 90 秒、宽限期内重连取消、再次全部断线重新计时、重复 close 不延长计时、到期仅终结一次。
- [x] 4.2 在房间领域对象中实现单个幂等宽限计时器的开始、取消和清理行为；由断线处理器仅在玩家断线使已开局普通房间无人连接时启动，由成功接管取消，并由统一终结流程无条件清理。
- [x] 4.3 在计时器到期回调中重新检查 `finalizing` 与玩家连接状态后调用现有统一终结服务，补充 started/cancelled/expired 结构化日志，并使 4.1 测试通过。
- [x] 4.4 回归等待阶段、匹配预留、WindBot/AI 和观战者断线测试，确认它们继续执行既有立即离房、中止、终结或仅移除观战者行为，不应用 90 秒宽限。

## 5. 真实 TCP 与跨传输回归

- [x] 5.1 先扩展真实 TCP 房间生命周期测试，在系统分配的 loopback 端口对 1103 和 1109 分别保持旧玩家 socket 打开，再以同 IP、同昵称和同房间连接，断言新连接恢复原席位、旧连接关闭且玩家数量不变。
- [x] 5.2 使用不同 loopback 来源地址构造跨 IP JOIN（环境不支持时以等价适配器集成测试替代），断言原玩家 socket 不被替换且新连接只能按既有阶段成为观战者或被拒绝。
- [x] 5.3 增加双方正常断线后在 90 秒内恢复的生命周期测试，并验证超时后相同房间标识只能创建新房间，旧对局状态和重连资格已被统一清理。
- [x] 5.4 运行现有 WebSocket token 重连、未知 token 拒绝、协议版本拒绝、观战和双环境同号隔离测试，确认普通 TCP 放宽没有改变其他入口。

## 6. 完整验证与上线准备

- [x] 6.1 运行所有重连、房间生命周期、断线处理和 socket 适配器聚焦测试，检查没有悬挂 timer、open handle、重复关闭或未处理 Promise。
- [x] 6.2 运行 `npm run lint`、`npm run test`、`npm run check:nostalgia-resources` 和 `npm run build`，记录结果并修复仅由本变更引起的回归。
- [x] 6.3 在隔离实例上运行 `npm run smoke:duel -- <port>` 验证 1103/1109 完整决斗与观战，再执行一次真实同 IP 半开重连演练并核对结构化日志时间线。
- [ ] 6.4 发布前查询活跃对局，选择无活跃房间窗口部署；上线后观察接管成功、IP 拒绝、宽限期开始/取消/到期计数，并保留上一镜像作为无数据迁移回滚点。
