## 1. 页面路由与准入边界

- [ ] 1.1 先为三 tab 页面 controller 和路由增加失败测试，覆盖无需登录访问 1103/1109、页面显示实际格式与三个 tab 结构、页面标题与文案使用 Nostalgia Duel Server 品牌且不含 SRVPro 等外部品牌与语言切换文案或控件、未知格式返回不存在、`RANK_ENABLED=false` 返回明确不可用、不暴露账号 ID/`userId`/PIN/IP/密码字段。
- [ ] 1.2 实现单一参数化 `GET /leaderboards/:format` 页面 controller 并注册路由，复用固定格式注册表与排位开关；只为已启用格式返回包含房间列表、录像下载、天梯排行三个 tab 的页面，不增加格式专用 controller 或路由。
- [ ] 1.3 增加页面/API 契约测试，固定页面只消费 `/api/getrooms`、`/api/replays/:format`（含下载）与 `/api/leaderboards/:format`，各 tab 请求均携带当前格式且不跨格式。

## 2. 排位房观战入口

- [ ] 2.1 先为观战标识识别增加失败测试，覆盖 `1103#TT4821`/`1109#TT4821` 命中观战分支、裸 `TT` 与 `format#TT` 仍走 PIN 匹配路径、大小写变体/额外分段/超长输入被拒绝，以及观战输入不落入普通数字房号策略、不创建普通房。
- [ ] 2.2 先为观战准入增加失败测试，覆盖无 PIN 观战者进入对局中与换备中的排位房被以 OBSERVER 准入并收到观战视角消息与历史小局重放、不写入占用与预留；等待匹配中与终结中的房间拒绝且提示不含等待玩家昵称；不存在或格式不匹配的观战号拒绝且无房间副作用；内部 ID 碰撞时取第一个对局中的房间。
- [ ] 2.3 实现排位加入策略的观战分支：`^\d{4}#TT\d+$` 语法在普通数字房号策略之前接管，按 `formatId` + `isDirectRanked` + 房间内部 ID + 非 finalizing 查找，对局中/换备中复用既有观战准入，其余状态以 YGOPro 原生序列化错误拒绝。
- [ ] 2.4 先为观战者隔离增加失败测试，覆盖 `isDirectRanked` 房间中观战者的 TO_DUEL/入座转正请求被阻断、观战者进出不影响 MATCH 终结结算与积分统计、下一小局开始后观战者仍在观众席。
- [ ] 2.5 实现排位房观战者转正阻断，并验证结算与统计隔离。

## 3. 排位录像只读 API

- [ ] 3.1 先为录像列表 API 增加失败测试，覆盖按结束时间倒序分页、响应不含 bytea 二进制、`total` 计数、`search` 昵称子串匹配与 `%`/`_` 通配符转义、1103/1109 格式隔离、anulled 比赛录像排除、空列表、`RANK_ENABLED=false` 返回不可用与未知格式拒绝。
- [ ] 3.2 实现 `GET /api/replays/:format`：只读 `duel_replays` ⋈ `duels`（经 `matches` 排除无效比赛），SQL 侧以 `octet_length` 计算大小，参数化搜索，默认每页 20 条。
- [ ] 3.3 先为录像下载 API 增加失败测试，覆盖返回字节与写入的 `.yrp` 一致且可解析、`Content-Disposition` 文件名含北京时间结束时间与双方昵称并过滤路径/控制字符、replayId 不存在或与路径格式不匹配返回不存在、排位关闭返回不可用。
- [ ] 3.4 实现 `GET /api/replays/:format/:replayId` 流式下载，校验录像归属格式，响应不含 `userId`、IP 或账号字段。

## 4. 排行榜 API 增量扩展

- [ ] 4.1 先为排行榜扩展增加失败测试，覆盖 `search` 昵称子串匹配、`page`/`pageSize` 分页与 `total` 字段、月榜与总榜分页正确性、非法参数拒绝，以及缺省新参数时响应与排序与既有契约完全一致的回归。
- [ ] 4.2 在 controller、查询用例与 PostgreSQL 仓库层增量实现 `search`/`page`/`pageSize`/`total`，参数化查询，不修改默认排序、响应既有字段与 `nostalgia-ranked-play` 已固定的契约。

## 5. 页面三 tab 行为

- [ ] 5.1 先为房间列表 tab 增加失败测试，覆盖按 `formatId` 过滤房间与统计行（房间总数、对局中、在线玩家、更新时间）、排位房类型标记与观战号展示、未开打排位房玩家列显示「等待匹配中」而不显示昵称、普通房复制 `format#roomId`、排位房复制 `format#TT<观战号>`（观战号取自 `/api/getrooms` 的 `roomid`）与手动刷新。
- [ ] 5.2 实现房间列表 tab：消费 `/api/getrooms`，展示房名、类型、模式、玩家、状态与统计行，提供普通房加入标识与排位房观战号复制及手动刷新，无自动轮询。
- [ ] 5.3 先为录像下载 tab 增加失败测试，覆盖列表行（时间、对战双方、大小、下载链接）、每页 20 条与首/末页翻页按钮禁用、搜索回车与按钮触发并回到第 1 页、清空恢复完整列表、空列表提示与失败重试。
- [ ] 5.4 实现录像下载 tab：分页、搜索、页码信息与刷新本页，下载入口指向录像下载 API。
- [ ] 5.5 先为天梯排行 tab 增加失败测试，覆盖任意本地时区下按 `Asia/Shanghai` 得到当前 `YYYY-MM`、首次加载月榜、总榜不携带 season、任意历史月份、每页 50 条分页、搜索显示真实昵称、无搜索时名次/总数 ≥ 0.7 的行昵称显示 `******`、总场次等于胜场加败场、前三名高亮类名，以及较早响应不覆盖最后一次选择。
- [ ] 5.6 实现天梯排行 tab：总榜/月榜切换、月份控件、搜索、分页、展示层打码与前三名高亮，列为排名、玩家、总积分、总场次、胜场、败场、胜率。
- [ ] 5.7 先为共享状态与安全渲染增加失败测试，覆盖三个 tab 各自的加载中/空数据/失败重试状态、失败时清除旧数据、包含 HTML 字符的昵称/房名/玩家名按纯文本显示、不渲染 `userId`。
- [ ] 5.8 实现每 tab 独立状态与递增请求序号，所有用户数据经 `textContent` 等纯文本 DOM API 写入。

## 6. 样式、复用、文档与验收

- [ ] 6.1 增加自包含且不依赖外部字体、CDN 或前端框架的三 tab 页面样式，保持桌面可读，并让不超过 480 像素的窄屏可横向滚动访问全部表格列与所有切换、搜索、分页、月份控件。
- [ ] 6.2 增加参数化复用测试，证明 1103/1109 使用同一 HTML、脚本和样式，未来注册格式可复用同一路径且三个 tab 请求均不跨格式。
- [ ] 6.3 更新可访问页面与排位文档，说明 `/leaderboards/:format` 三 tab 结构、排位房观战号 `format#TT<观战号>` 及其仅对局中可观战/永不转座的规则、录像列表/下载 API、排行榜 `search`/`page`/`pageSize`/`total` 扩展、默认北京时间月榜、后 30% 昵称展示层打码、未知格式与排位关闭行为，并明确页面依赖已落地的排位后端。
- [ ] 6.4 运行页面、观战与 API 聚焦测试，再运行 `npm run lint`、`npm run test`、`npm run check:nostalgia-resources` 和 `npm run build`，修复本变更引入的回归。
- [ ] 6.5 在 `RANK_ENABLED=true` 环境完成 1103/1109 三个 tab 验收：房间过滤/复制（含观战号）/手动刷新、录像搜索/分页/下载 `.yrp` 可被客户端解析、天梯当前月/历史月/总榜/搜索/分页/打码/失败重试；用真实客户端或测试 socket 验收对局中排位房观战进入、等待房观战被拒与观战者不转正；在排位关闭、未知格式、桌面和窄屏环境完成对应拒绝与布局验收。
