## 1. 页面路由与准入边界

- [ ] 1.1 先为排行榜页面 controller 和路由增加失败测试，覆盖无需登录访问 1103/1109、页面显示实际格式、未知格式返回不存在、`RANK_ENABLED=false` 返回明确不可用，以及不暴露账号 ID、PIN、密码或录像字段。
- [ ] 1.2 实现单一参数化 `GET /leaderboards/:format` 页面 controller 并注册路由，复用固定格式注册表与排位开关；只为已启用格式返回页面，不增加格式专用 controller 或路由。
- [ ] 1.3 增加页面/API 契约测试，固定页面只消费 `/api/leaderboards/:format` 的 `format/scope/season` 与 `rank/name/points/wins/losses`，并确认 `add-direct-nostalgia-ranked-rooms` 的排行榜 API 就绪后才允许页面上线。

## 2. 榜单切换、请求与安全渲染

- [ ] 2.1 先为页面请求状态增加失败测试，覆盖任意浏览器本地时区下按 `Asia/Shanghai` 得到当前 `YYYY-MM`、首次加载当前月榜、切换总榜不携带 season、选择其他月份，以及较早响应不能覆盖最后一次选择。
- [ ] 2.2 使用原生 JavaScript 实现最小页面状态和同源 fetch：维护格式、scope、season、加载状态与请求序号，严格生成月榜/总榜 URL，并让重试重复当前选择。
- [ ] 2.3 先为表格与反馈状态增加失败测试，覆盖有数据、负积分、空榜、HTTP/网络失败、重试，以及包含 HTML 字符的昵称不会被解释为标记或脚本。
- [ ] 2.4 实现排名、昵称、积分、胜场、负场表格和加载/空榜/失败状态；所有排行字段通过 DOM 纯文本 API 写入，失败或空榜时清除上一笔数据。

## 3. 页面样式、复用与验收

- [ ] 3.1 增加自包含且不依赖外部字体、CDN 或前端框架的页面样式，保持桌面可读，并让不超过 480 像素的窄屏可横向访问全部表格列和所有切换控件。
- [ ] 3.2 增加参数化复用测试，证明 1103/1109 使用同一 HTML、脚本和样式，未来注册格式可复用同一路径且请求不跨格式。
- [ ] 3.3 更新可访问页面与排位文档，说明 `/leaderboards/:format`、默认当前北京时间月榜、总榜/月榜切换、未知格式和排位关闭行为，并明确页面依赖后端排位 API。
- [ ] 3.4 运行页面聚焦测试，再运行 `npm run lint`、`npm run test`、`npm run check:nostalgia-resources` 和 `npm run build`，修复本变更引入的回归。
- [ ] 3.5 在 `RANK_ENABLED=true` 环境完成 1103/1109 当前月、其他月份、总榜、空榜、失败重试和昵称安全渲染验收；在排位关闭、未知格式、桌面和窄屏环境完成对应页面验收。
