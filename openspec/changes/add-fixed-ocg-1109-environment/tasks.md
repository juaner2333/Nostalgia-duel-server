## 1. 固定双环境资源基线

- [ ] 1.1 读取 `706-cards.cdb`，记录文件 SHA-256、5120 个有效卡片 ID、集合摘要和异常/重复 ID 报告，作为生成与验收基线
- [ ] 1.2 解析 `1103-diff (1).ydk` 与 `1103-diff (2).ydk` 的全部 YDK 分区，记录各自 SHA-256、58/60 个 ID、无交集且均属于 706 集合的断言，并锁定 1103 最终 5002 个 ID 的集合摘要
- [ ] 1.3 校验并固化 `2011.03.lflist` 与 `2011.09.lflist` 的 SHA-256、标题、0/1/2 限制条目、重复项及数据库外 ID 诊断，确认两份输入分别绑定 1103 与 1109
- [ ] 1.4 确定基础脚本和 1103/1109 覆盖脚本快照的来源版本，列出 706 卡片脚本与公共初始化/过程脚本的筛选规则、format 差异及缺失依赖报告
- [ ] 1.5 创建受控 `nostalgia-resources` 目录和定向 `.gitignore` 例外，将五个固定输入、共享数据库及 base/format 脚本快照纳入版本控制，不放开其他生成资源

## 2. 生成并锁定 1103 与 1109 禁限卡表

- [ ] 2.1 先为生成器添加同目录测试 fixture，覆盖 CDB/LFList/YDK 解析、默认 3、0/1/2 覆盖、1103 集合差、1109 完整集合、重复/非法 ID、数据库外限制 ID、稳定排序和 `--check` 漂移，并确认测试先失败
- [ ] 2.2 使用现有 `sql.js` 与 LFList 工具实现单一确定性生成命令，按 format 接收数据库、限制输入和可选排除集，不引入第三方依赖
- [ ] 2.3 实现 1103 生成路径：以 706 集合减去两个 YDK 并集为 whitelist 全集，应用 `2011.03.lflist` 限制并输出 `formats/1103/lflist.conf`
- [ ] 2.4 实现 1109 生成路径：以完整 706 集合为 whitelist 全集，应用 `2011.09.lflist` 限制并输出 `formats/1109/lflist.conf`
- [ ] 2.5 增加生成后校验，断言两份输出分别为 5002/5120 个唯一 ID、数量仅为 0–3、集合严格等于对应卡池，并计算 YGOPro LFList hash 与 SHA-256
- [ ] 2.6 生成并提交两份 `lflist.conf`，审查包括 `82301904` 在内的数据库外限制诊断，运行双环境 `--check` 确认逐字节可重现
- [ ] 2.7 生成 `nostalgia-resources/lock.json`，记录五个输入、数据库/排除/最终卡池集合、base/format 脚本和双 LFList 摘要，并为 lock 漂移与缺失文件添加失败测试

## 3. 支持完全本地的固定资源组装

- [ ] 3.1 先扩展资源清单测试，覆盖 `local` source 的相对路径、绝对路径拒绝、`..`/符号链接逃逸拒绝、无网络拉取和明确 assembly 文件/目录解析，并确认测试先失败
- [ ] 3.2 在资源清单验证与拉取/组装脚本中实现受限 `local` source，保持候选 release 与原子 symlink 发布行为
- [ ] 3.3 为候选 release 添加 lock、双环境卡池集合、双 LFList 和必需脚本完整性门禁；测试任一文件或摘要失配时 `resources/current` 保持不变
- [ ] 3.4 将 `resources.manifest.json` 改为唯一 `nostalgia-fixed` 本地 source，只组装 base 与 1103/1109 资源，并将运行时结构改为 `base + formats.1103 + formats.1109`
- [ ] 3.5 增加资源图断言，证明 manifest 不含 Git/HTTP source、发布树不依赖外部仓库缓存，且干净应用构建产物可在断网状态完成资源组装和启动
- [ ] 3.6 更新资源版本接口，使其报告固定资源 schema/version、lock、数据库集合及 1103/1109 卡池与 LFList 摘要，并添加接口契约测试

## 4. 隔离双环境卡池、禁限卡表和脚本

- [ ] 4.1 先修改资源解析器测试，将旧 standard/extended 断言替换为固定 base 与两个显式 format 映射，并覆盖未知/缺失 format 的失败行为
- [ ] 4.2 实现 base 只加载一个固定 CDB，并按 format 应用卡池集合；用集成测试证明 1103/1109 内存 ID 集合分别等于 5002/5120 基线且不加载其他数据库
- [ ] 4.3 先为 `formatId -> banListHash` 添加测试，再实现只从对应 format 目录加载 LFList，消除依赖加载顺序的数字索引
- [ ] 4.4 先用同名测试脚本锁定 reader 优先级和 base 回退，再让每场决斗只接收 `[currentFormatPath, basePath]`，覆盖 1103/1109 双向禁止跨 format 查找
- [ ] 4.5 将固定引擎预加载脚本纳入 base/format 资源和 lock 校验，移除未受摘要保护的外部脚本路径与 `YGOPRO_EXTRA_SCRIPTS` 注入
- [ ] 4.6 分别使用代表性 1103 与 1109 卡组启动真实 WASM 决斗，验证 format 覆盖、base 回退、缺失脚本日志、卡池差异与卡池外卡片拒绝

## 5. 实现双环境房间路由

- [ ] 5.1 先为怀旧 format 领域注册表添加同目录测试，覆盖 1103/1109 的 OCG `rule=0`、Master Rule 2、MATCH、8000 LP、best-of-3、卡池/LFList 绑定和不可变 format ID
- [ ] 5.2 实现最小双环境注册表与资源端口，使领域规则不依赖 manifest、文件系统或基础设施模块
- [ ] 5.3 先为环境化加入策略添加测试，覆盖 1103/1109 首人创建、次人加入、无密码、同号隔离、未知环境、空/非数字/多段/超长 roomId，并确认非法输入不会回退到默认名称/密码策略
- [ ] 5.4 在 AI/重连专用策略之后、默认策略之前实现 `1103|1109#<roomId>` 加入策略和专用房间工厂，保存 `formatId`、外部 `roomId`、组合 admission key 与空密码
- [ ] 5.5 为房间仓库增加组合环境键查找并保留内部数值 ID；用测试证明 `1103#1001` 与 `1109#1001` 的玩家、消息和生命周期互不影响
- [ ] 5.6 更新房间 API、日志、录像和持久化元数据，一致展示/保存外部环境化 ID 与对应 LFList hash，同时保持内部 ID 关联能力

## 6. 保持观战与重连行为

- [ ] 6.1 先添加环境化房间观战回归测试，分别覆盖等待、猜拳、选择先后手、决斗中和换备阶段的第三客户端加入，并确认测试在路由实现前失败
- [ ] 6.2 让新路由只负责解析 format、查找/创建房间并发出既有加入动作，由现有状态处理器继续判定玩家、重连玩家或观战者
- [ ] 6.3 测试后加入观战者收到允许公开的历史视图与实时观战消息，但收不到手牌等仅玩家可见信息，不占席位也不改变准备、卡组或比赛状态
- [ ] 6.4 测试符合条件的重连玩家优先恢复原席位，等待状态观战转玩家行为保持不变，观战者断开只更新人数且不销毁房间
- [ ] 6.5 用两个同 roomId 的 1103/1109 房间并行测试观战隔离，确认观战历史、实时消息、人数、断开和房间结束事件均不跨环境

## 7. 删除未启用赛制与所有上游资源依赖

- [ ] 7.1 从 manifest 删除 ProjectIgnis LFLists、evolution lflists/assets、Moecube CDB/LFList、Fluorohydride scripts、预发布、自定义卡和 Edison core source 及其 assembly/runtime 消费者
- [ ] 7.2 删除 MD、Tengu、HAT、Edison、JTP、World、Genesys、Rush、Speed、GOAT、现代 OCG、PRE/ART 等未启用格式映射和 extended-card-pool 分支，并清理本次产生的未使用 imports、配置和测试 fixture
- [ ] 7.3 移除网络资源更新 sidecar/循环、外部资源仓库缓存消费者和额外脚本注入，只保留一次性本地组装及必要的进程内本地版本切换
- [ ] 7.4 更新 Docker/Compose、入口脚本和运维配置，确保运行时不克隆、下载或刷新任何卡库、脚本、禁限卡表或专用 core；stock WASM core 继续来自已安装的应用 npm 依赖
- [ ] 7.5 更新 README、环境变量示例和运行手册，只声明 `1103#roomId`、`1109#roomId`、固定资源布局、双环境生成/check、审核升级流程，以及“应用依赖安装不属于资源离线保证”的边界
- [ ] 7.6 增加最终资源图断言，证明生效 source 只有 `nostalgia-fixed`，发布树只有 base/1103/1109 所需内容，无 external source、extended 目录、旧仓库缓存消费者和动态脚本入口

## 8. 完整验证与发布演练

- [ ] 8.1 运行双环境 LFList `--check`、lock 校验、manifest 严格校验及全部聚焦测试，确认生成文件、卡池数量和固定摘要无漂移
- [ ] 8.2 运行 `npm run lint`、`npm run test` 和 `npm run build`，修复本变更造成的所有失败且不改动无关代码
- [ ] 8.3 从干净且依赖已安装的检出断网执行固定资源组装和服务启动，验证无 Git/HTTP 资源请求、原子发布、损坏候选回滚及资源版本接口
- [ ] 8.4 用两个客户端通过 `1103#1001` 完成含换备的三局两胜，并以第三客户端验证五个阶段观战、重连、OCG 2011.03 禁卡、1103 排除卡、录像与脚本裁定
- [ ] 8.5 用两个客户端通过 `1109#1001` 完成含换备的三局两胜，并以第三客户端验证五个阶段观战、重连、OCG 2011.09 禁卡、完整 706 卡池、录像与脚本裁定
- [ ] 8.6 并行保留 `1103#1001` 与 `1109#1001`，验证玩家、观战者、消息、录像和结束事件隔离，再演练应用镜像与 `resources/current` 双回滚
